export enum SignupStatus {
  PENDING = "PENDING",
  ACCEPTED = "ACCEPTED",
  REJECTED = "REJECTED",
}

export enum ROLES {
  ADMIN = "ADMIN",
  USER = "USER",
}

export enum BORROWER_STATUS {
  BORROWED = "BORROWED",
  RETURNED = "RETURNED",
}
